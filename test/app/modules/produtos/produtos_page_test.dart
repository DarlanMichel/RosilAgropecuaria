import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:rosilagropecuaria/app/modules/produtos/produtos_page.dart';

void main() {
  testWidgets('ProdutosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProdutosPage(title: 'Produtos')));
    //  final titleFinder = find.text('Produtos');
    //  expect(titleFinder, findsOneWidget);
  });
}
