import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:rosilagropecuaria/app/modules/carrinho/carrinho_page.dart';

void main() {
  testWidgets('CarrinhoPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CarrinhoPage(title: 'Carrinho')));
    //  final titleFinder = find.text('Carrinho');
    //  expect(titleFinder, findsOneWidget);
  });
}
