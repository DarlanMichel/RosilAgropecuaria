import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:rosilagropecuaria/app/modules/login/widget/header/header_controller.dart';
import 'package:rosilagropecuaria/app/app_module.dart';

void main() {
  initModule(AppModule());
  // HeaderController header;
  //
  setUp(() {
    //     header = AppModule.to.get<HeaderController>();
  });

  group('HeaderController Test', () {
    //   test("First Test", () {
    //     expect(header, isInstanceOf<HeaderController>());
    //   });

    //   test("Set Value", () {
    //     expect(header.value, equals(0));
    //     header.increment();
    //     expect(header.value, equals(1));
    //   });
  });
}
