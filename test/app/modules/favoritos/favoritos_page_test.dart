import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:rosilagropecuaria/app/modules/favoritos/favoritos_page.dart';

void main() {
  testWidgets('FavoritosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FavoritosPage(title: 'Favoritos')));
    //  final titleFinder = find.text('Favoritos');
    //  expect(titleFinder, findsOneWidget);
  });
}
