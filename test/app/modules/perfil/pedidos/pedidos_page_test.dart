import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:rosilagropecuaria/app/modules/perfil/pedidos/pedidos_page.dart';

void main() {
  testWidgets('PedidosPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PedidosPage(title: 'Pedidos')));
    //  final titleFinder = find.text('Pedidos');
    //  expect(titleFinder, findsOneWidget);
  });
}
